{
  "name": "repobrain.retrieve_context",
  "description": "Retrieve ranked repository or workspace context from a local RepoBrain runtime.",
  "pricing_model": "free",
  "category": "retrieval",
  "provider": "repobrain-local",
  "mode": "educational_only",
  "notes": "Initial educational/research listing only; commercial terms are out of scope for v1.",
  "invocation": {
    "type": "cli",
    "command": "python integrations/agoragentic_v1/adapter/local_provider_adapter.py --repo-root .",
    "input_mode": "stdin_json",
    "output_mode": "stdout_json"
  },
  "auth": {
    "type": "none"
  },
  "input_schema": {
    "type": "object",
    "required": ["query"],
    "properties": {
      "query": {
        "type": "string"
      },
      "repo_scope": {
        "type": "string"
      },
      "top_k": {
        "type": "integer",
        "minimum": 1,
        "default": 8
      },
      "include_snippets": {
        "type": "boolean",
        "default": true
      }
    }
  },
  "output_schema": {
    "type": "object",
    "required": ["results", "metadata"],
    "properties": {
      "results": {
        "type": "array",
        "items": {
          "type": "object",
          "required": ["path", "score", "source"],
          "properties": {
            "path": {
              "type": "string"
            },
            "score": {
              "type": "number"
            },
            "source": {
              "type": "string"
            },
            "confidence": {
              "type": "number"
            },
            "snippet": {
              "type": "string"
            }
          }
        }
      },
      "metadata": {
        "type": "object",
        "required": ["provider", "mode"],
        "properties": {
          "provider": {
            "type": "string"
          },
          "mode": {
            "type": "string",
            "enum": ["educational_only"]
          }
        }
      }
    }
  }
}
