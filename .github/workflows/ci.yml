name: CI

on:
  push:
    branches: ["master", "main"]
  pull_request:
    branches: ["master", "main"]

permissions:
  contents: read

jobs:
  test:
    name: Python ${{ matrix.python-version }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.12", "3.13"]

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: |
            webapp/package-lock.json
            docs-for-repobrain/package-lock.json

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install frontend dependencies
        working-directory: webapp
        run: npm ci

      - name: Build frontend assets
        working-directory: webapp
        run: npm run build

      - name: Install docs frontend dependencies
        working-directory: docs-for-repobrain
        run: npm ci

      - name: Lint docs frontend
        working-directory: docs-for-repobrain
        run: npm run lint

      - name: Build docs frontend
        working-directory: docs-for-repobrain
        run: npm run build

      - name: Install package
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[dev,tree-sitter]"

      - name: Compile package
        run: python -m compileall src testsuite

      - name: Lint Python (ruff)
        run: python -m ruff check src testsuite --select E9,F63,F7,F82

      - name: Type check critical modules (mypy)
        run: python -m mypy

      - name: Run tests with coverage gate
        run: python -m pytest -q testsuite --cov=repobrain.engine.provider_base --cov=repobrain.models --cov-report=term-missing --cov-fail-under=80
